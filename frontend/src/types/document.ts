export interface Document {
  id: string;
  filename: string;
  size: number;
  upload_date: string;
}

export interface UploadResponse {
  id: string;
  filename: string;
  size: number;
  upload_date: string;
}
